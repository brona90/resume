pipeline {
    agent any
    stages {
        
        stage('Checkout'){
            steps {
                // checkout scm
                sh 'ls -la'
            }
        }
        
        stage('Hello') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
